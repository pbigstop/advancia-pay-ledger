# Demo Page

The full demo HTML file should be placed here as `index.html`.

Copy the demo HTML from your Downloads folder:
```bash
cp ~/Downloads/advancia-demo.html demo/index.html
```

This is the standalone investor demo page that runs entirely client-side with no backend dependency.
